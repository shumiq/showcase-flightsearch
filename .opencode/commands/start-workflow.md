---
description: Start a workflow from .opencode/workflows
agent: technical-lead
---
Load the `start-workflow` skill and follow its instructions. If no workflow is specified, prompt the user to choose one.

Workflow / request: $ARGUMENTS
