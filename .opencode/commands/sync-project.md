---
description: Sync project documentation with codebase state
agent: technical-lead
---
Load the `sync-project` skill and follow its instructions. Synchronize Confluence project pages and local documentation files (AGENTS.md, README.md) with the current state of the codebase. By default sync changed components only; pass "full" as argument for a full sync.

Mode: $ARGUMENTS
